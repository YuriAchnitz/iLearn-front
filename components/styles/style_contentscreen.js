import { StyleSheet } from 'react-native';
import { colors } from './colors';

export default StyleSheet.create(
    {
        container_general: {
            padding: 20,
            alignItems: 'center',
            alignContent: 'center',
            textAlign: 'center'
        },


    }
);